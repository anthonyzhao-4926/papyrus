# Papyrus Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个静态站点博客 Papyrus —— 从多个 GitHub repo 聚合 markdown 文档展示为博客，纯阅读、零后端、PC-only。

**Architecture:** Astro SSG。构建期通过 GitHub API 拉取所有已配置 repo 的 `*.md` 文件 → remark/rehype 渲染 HTML → 输出静态资产 + Pagefind 索引 → 部署至 Vercel。前端三种页面（首页 Repo Grid / Repo 页 / 文章页三栏），暗色模式 + ⌘K 搜索。

**Tech Stack:** Astro 5 · Tailwind CSS 3 · TypeScript 5 · pnpm · Octokit · Shiki · KaTeX · Pagefind · medium-zoom · Vitest · Playwright · Zod · MSW

**Spec:** `docs/superpowers/specs/2026-06-05-papyrus-design.md`

---

## File Structure

| 路径 | 职责 |
|---|---|
| `package.json` | 依赖 / 脚本 |
| `tsconfig.json` | TS 配置 |
| `astro.config.mjs` | Astro 集成（Tailwind / Sitemap / Pagefind） |
| `tailwind.config.ts` | 设计 tokens |
| `papyrus.config.json` | 用户配置（站点元数据 + repos） |
| `.env.example` | `GITHUB_TOKEN` |
| `src/lib/types.ts` | 全局类型 |
| `src/lib/config.ts` | Zod schema + 读取 / 校验 config |
| `src/lib/tree.ts` | 扁平路径 → 嵌套树 |
| `src/lib/github.ts` | 唯一接触 GitHub API 的模块 |
| `src/lib/content-loader.ts` | config × github → 归一化文章 |
| `src/lib/markdown.ts` | remark/rehype 渲染管道 |
| `src/lib/images.ts` | 下载并缓存 repo 图片 |
| `src/styles/global.css` | Tailwind base + CSS 变量 |
| `src/styles/prose.css` | 文章正文排版 |
| `src/layouts/BaseLayout.astro` | `<html>` + 暗色模式 inline script + SiteHeader |
| `src/layouts/HomeLayout.astro` | 首页 layout |
| `src/layouts/RepoLayout.astro` | Repo 页 layout（侧栏 + 列表） |
| `src/layouts/ArticleLayout.astro` | 文章页 layout（三栏） |
| `src/components/SiteHeader.astro` | 顶栏 Logo / 导航 / 搜索 / 暗色切换 |
| `src/components/ThemeToggle.astro` | 浅 / 深 / 跟随系统 |
| `src/components/RepoCard.astro` | 首页 repo 卡片 |
| `src/components/RepoTree.astro` | 左侧递归树 |
| `src/components/ArticleTOC.astro` | 右侧文章目录 |
| `src/components/ArticleList.astro` | 文章列表行 |
| `src/components/SearchModal.astro` | ⌘K Pagefind 弹窗 |
| `src/components/Prose.astro` | 文章正文容器 |
| `src/pages/index.astro` | 首页 |
| `src/pages/[repo]/index.astro` | Repo 入口 |
| `src/pages/[repo]/[...path].astro` | 文章详情 |
| `src/pages/404.astro` | 404 |
| `scripts/prebuild.ts` | 构建前拉取 repos 到本地缓存 |
| `tests/fixtures/repo-tree.json` | 假 GitHub tree 响应 |
| `tests/fixtures/articles/*.md` | 假 markdown 文件 |
| `tests/unit/tree.test.ts` | tree 单测 |
| `tests/unit/github.test.ts` | github API 单测（msw） |
| `tests/unit/markdown.test.ts` | markdown 渲染 snapshot |
| `tests/e2e/smoke.spec.ts` | 端到端 smoke |
| `playwright.config.ts` | Playwright 配置 |
| `vitest.config.ts` | Vitest 配置 |
| `README.md` | 使用 / 部署文档 |

---

## Task 1: 项目骨架与依赖

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `astro.config.mjs`
- Create: `.env.example`
- Create: `README.md`
- Modify: `.gitignore`

- [ ] **Step 1: 写 `package.json`**

```json
{
  "name": "papyrus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "tsx scripts/prebuild.ts && astro build && pagefind --site dist",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "astro check && tsc --noEmit"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/tailwind": "^5.1.4",
    "@octokit/rest": "^21.1.0",
    "astro": "^5.1.0",
    "medium-zoom": "^1.1.0",
    "pagefind": "^1.3.0",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-katex": "^7.0.1",
    "rehype-slug": "^6.0.0",
    "remark-math": "^6.0.0",
    "shiki": "^1.26.0",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "msw": "^2.7.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.15.0"
}
```

- [ ] **Step 2: 写 `tsconfig.json`**

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist", "node_modules"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~/*": ["src/*"]
    },
    "types": ["vitest/globals", "node"]
  }
}
```

- [ ] **Step 3: 写 `astro.config.mjs`**

```js
import { defineConfig } from "astro/config";
import tailwind from "@astrojs/tailwind";
import sitemap from "@astrojs/sitemap";

export default defineConfig({
  site: "https://blog.example.com",
  integrations: [tailwind({ applyBaseStyles: false }), sitemap()],
  markdown: {
    syntaxHighlight: false, // 我们自己用 shiki + remark/rehype 管道
  },
  vite: {
    resolve: {
      alias: { "~": new URL("./src", import.meta.url).pathname },
    },
  },
});
```

- [ ] **Step 4: 写 `.env.example`**

```
# GitHub Personal Access Token，public_repo 范围即可
# 未设置时会用未认证请求（60/h），大概率不够用
GITHUB_TOKEN=
```

- [ ] **Step 5: 写最小 `README.md`**

```markdown
# Papyrus

把多个 GitHub repo 里的 markdown 笔记，零运维地聚合成一个静态博客。

详见 `docs/superpowers/specs/2026-06-05-papyrus-design.md`。

## 开发

\`\`\`bash
pnpm install
cp .env.example .env  # 填入 GITHUB_TOKEN
pnpm dev
\`\`\`

## 构建

\`\`\`bash
pnpm build
\`\`\`
```

- [ ] **Step 6: 追加 `.gitignore`**

```
node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
public/images/
.superpowers/
.cache/
playwright-report/
test-results/
```

- [ ] **Step 7: 安装依赖**

Run: `pnpm install`
Expected: 创建 `node_modules/` 和 `pnpm-lock.yaml`，无 ERR 输出。

- [ ] **Step 8: 跑 typecheck 确认基础结构有效**

Run: `pnpm typecheck 2>&1 || true`
Expected: 因为还没有 `src/`，Astro 可能报"no pages"警告，可接受。无 TS 错误。

- [ ] **Step 9: Commit**

```bash
git add package.json tsconfig.json astro.config.mjs .env.example README.md .gitignore pnpm-lock.yaml
git commit -m "chore: 初始化 Astro + Tailwind + TypeScript 项目骨架"
```

---

## Task 2: 设计 tokens 与 global styles

**Files:**
- Create: `tailwind.config.ts`
- Create: `src/styles/global.css`
- Create: `src/styles/prose.css`

- [ ] **Step 1: 写 `tailwind.config.ts`**

```ts
import type { Config } from "tailwindcss";

export default {
  content: ["./src/**/*.{astro,html,ts,tsx,js,jsx,md,mdx}"],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        bg: "var(--c-bg)",
        "bg-elev": "var(--c-bg-elev)",
        border: "var(--c-border)",
        text: "var(--c-text)",
        "text-mute": "var(--c-text-mute)",
        "text-faint": "var(--c-text-faint)",
        accent: "var(--c-accent)",
        "code-bg": "var(--c-code-bg)",
      },
      fontFamily: {
        sans: ['-apple-system', '"Segoe UI"', '"PingFang SC"',
               '"Hiragino Sans GB"', '"Microsoft YaHei"', 'sans-serif'],
        mono: ['"SF Mono"', 'Menlo', 'Consolas', '"Liberation Mono"', 'monospace'],
      },
      maxWidth: {
        prose: "720px",
        page: "1280px",
      },
    },
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 2: 写 `src/styles/global.css`**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --c-bg: #ffffff;
  --c-bg-elev: #fafafa;
  --c-border: #f1f5f9;
  --c-text: #1f2937;
  --c-text-mute: #64748b;
  --c-text-faint: #94a3b8;
  --c-accent: #2563eb;
  --c-code-bg: #0f172a;
}

.dark {
  --c-bg: #0b0d10;
  --c-bg-elev: #15181d;
  --c-border: #1f2937;
  --c-text: #e5e7eb;
  --c-text-mute: #94a3b8;
  --c-text-faint: #64748b;
  --c-accent: #60a5fa;
  --c-code-bg: #0b0d10;
}

html {
  background: var(--c-bg);
  color: var(--c-text);
  font-family: theme("fontFamily.sans");
  -webkit-font-smoothing: antialiased;
}

body { margin: 0; }
a { color: inherit; text-decoration: none; }
```

- [ ] **Step 3: 写 `src/styles/prose.css`**

```css
.prose {
  color: var(--c-text);
  line-height: 1.72;
  font-size: 15px;
}
.prose h1 { font-size: 30px; line-height: 1.25; font-weight: 700; margin: 0 0 8px; letter-spacing: -0.01em; }
.prose h2 { font-size: 22px; line-height: 1.4; font-weight: 700; margin: 32px 0 8px; }
.prose h3 { font-size: 17px; line-height: 1.4; font-weight: 600; margin: 22px 0 6px; }
.prose p { margin: 0 0 12px; }
.prose ul, .prose ol { padding-left: 22px; margin: 8px 0; }
.prose li { margin-bottom: 4px; }
.prose code:not(pre code) {
  background: var(--c-border);
  color: var(--c-text);
  padding: 2px 6px;
  border-radius: 4px;
  font-size: 13.5px;
  font-family: theme("fontFamily.mono");
}
.prose pre {
  background: var(--c-code-bg);
  color: #e2e8f0;
  padding: 16px 18px;
  border-radius: 8px;
  font-size: 13px;
  line-height: 1.6;
  overflow-x: auto;
  margin: 14px 0;
  font-family: theme("fontFamily.mono");
}
.prose blockquote {
  background: var(--c-bg-elev);
  border-left: 3px solid var(--c-accent);
  padding: 10px 16px;
  color: var(--c-text-mute);
  margin: 14px 0;
  border-radius: 0 6px 6px 0;
}
.prose table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  border: 1px solid var(--c-border);
  border-radius: 6px;
  overflow: hidden;
  margin: 14px 0;
  font-size: 13.5px;
}
.prose th, .prose td {
  padding: 10px 14px;
  text-align: left;
  border-bottom: 1px solid var(--c-border);
}
.prose th {
  background: var(--c-bg-elev);
  color: var(--c-text-mute);
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  font-weight: 600;
}
.prose tr:last-child td { border-bottom: none; }
.prose a { color: var(--c-accent); }
.prose a:hover { text-decoration: underline; }
.prose img { max-width: 100%; height: auto; border-radius: 6px; }
.prose .math-display { overflow-x: auto; padding: 12px 0; }
```

- [ ] **Step 4: Commit**

```bash
git add tailwind.config.ts src/styles/global.css src/styles/prose.css
git commit -m "feat(styles): 添加设计 tokens 与文章排版样式"
```

---

## Task 3: BaseLayout + SiteHeader + ThemeToggle（无 FOUC）

**Files:**
- Create: `src/layouts/BaseLayout.astro`
- Create: `src/components/SiteHeader.astro`
- Create: `src/components/ThemeToggle.astro`
- Create: `src/pages/index.astro`（暂时只放占位，验证布局）

- [ ] **Step 1: 写 `src/layouts/BaseLayout.astro`**

```astro
---
import "~/styles/global.css";
import "~/styles/prose.css";
import SiteHeader from "~/components/SiteHeader.astro";

interface Props {
  title: string;
  description?: string;
}
const { title, description = "" } = Astro.props;
---
<!doctype html>
<html lang="zh">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=1280" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <script is:inline>
      (() => {
        const saved = localStorage.getItem("theme");
        const sys = matchMedia("(prefers-color-scheme: dark)").matches;
        const dark = saved === "dark" || (!saved && sys);
        if (dark) document.documentElement.classList.add("dark");
      })();
    </script>
  </head>
  <body class="min-h-screen bg-bg text-text">
    <SiteHeader />
    <slot />
  </body>
</html>
```

- [ ] **Step 2: 写 `src/components/ThemeToggle.astro`**

```astro
---
---
<button
  id="theme-toggle"
  type="button"
  class="w-8 h-8 border border-border rounded-md flex items-center justify-center text-text-mute hover:bg-bg-elev"
  aria-label="切换主题"
>
  <svg id="icon-light" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-4 h-4 hidden dark:block"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/></svg>
  <svg id="icon-dark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-4 h-4 block dark:hidden"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
</button>
<script>
  const btn = document.getElementById("theme-toggle")!;
  btn.addEventListener("click", () => {
    const isDark = document.documentElement.classList.toggle("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
  });
</script>
```

- [ ] **Step 3: 写 `src/components/SiteHeader.astro`**

```astro
---
import ThemeToggle from "~/components/ThemeToggle.astro";
---
<header class="flex items-center px-8 py-4 border-b border-border gap-6">
  <a href="/" class="font-bold text-base tracking-tight">
    Papyrus<span class="text-accent">.</span>
  </a>
  <nav class="flex gap-5 text-sm text-text-mute">
    <a href="/" class="hover:text-text">首页</a>
  </nav>
  <div class="ml-auto flex items-center gap-3">
    <button
      id="open-search"
      type="button"
      class="flex items-center gap-2 bg-bg-elev px-3 py-1.5 rounded-md text-xs text-text-faint w-64"
      aria-label="搜索（⌘K）"
    >
      <span>🔍</span>
      <span>搜索文章...</span>
      <span class="ml-auto flex gap-1">
        <kbd class="bg-bg border border-border rounded px-1.5 text-[11px]">⌘</kbd>
        <kbd class="bg-bg border border-border rounded px-1.5 text-[11px]">K</kbd>
      </span>
    </button>
    <ThemeToggle />
  </div>
</header>
```

- [ ] **Step 4: 写占位首页 `src/pages/index.astro`**

```astro
---
import BaseLayout from "~/layouts/BaseLayout.astro";
---
<BaseLayout title="Papyrus" description="多 repo Markdown 聚合博客">
  <main class="max-w-page mx-auto px-8 py-16">
    <h1 class="text-3xl font-bold">Papyrus</h1>
    <p class="text-text-mute mt-2">骨架就位 — 后续任务填充 RepoCard 网格</p>
  </main>
</BaseLayout>
```

- [ ] **Step 5: 启动 dev server，肉眼验证**

Run（前台 30 秒后 Ctrl-C，或后台启动）:
```
pnpm dev
```
Expected: 控制台显示 `Local: http://localhost:4321/`，浏览器访问能看到"骨架就位"提示，背景白色，右上角有暗色切换按钮，点击切换不闪烁。

- [ ] **Step 6: Commit**

```bash
git add src/layouts/BaseLayout.astro src/components/SiteHeader.astro src/components/ThemeToggle.astro src/pages/index.astro
git commit -m "feat(ui): 实现 BaseLayout、SiteHeader、ThemeToggle（无 FOUC）"
```

---

## Task 4: 类型 + Zod schema + 示例配置

**Files:**
- Create: `src/lib/types.ts`
- Create: `src/lib/config.ts`
- Create: `papyrus.config.json`

- [ ] **Step 1: 写 `src/lib/types.ts`**

```ts
export interface SiteConfig {
  name: string;
  description: string;
  url: string;
}

export interface RepoConfig {
  slug: string;
  title: string;
  description: string;
  icon: string;
  source: string;     // github.com/{owner}/{repo}
  branch: string;     // 默认 main
}

export interface PapyrusConfig {
  site: SiteConfig;
  repos: RepoConfig[];
}

export interface Article {
  repo: string;          // repo slug
  path: string[];        // 不含 .md 的路径段，如 ["frontend", "react-hooks"]
  title: string;
  date: string;          // ISO8601
  rawMarkdown: string;
  icon?: string;
}

export interface TreeNode {
  name: string;          // 显示名（文件名 / 目录名）
  type: "file" | "dir";
  path: string[];        // 完整路径段（文件不含 .md 后缀）
  children?: TreeNode[];
  article?: Article;     // type === "file" 时挂载
}

export interface RenderedArticle extends Article {
  html: string;
  toc: TocItem[];
}

export interface TocItem {
  id: string;
  text: string;
  depth: number;         // 2 = h2, 3 = h3
}
```

- [ ] **Step 2: 写 `src/lib/config.ts`**

```ts
import { z } from "zod";
import { readFile } from "node:fs/promises";
import type { PapyrusConfig } from "./types.js";

const RepoSchema = z.object({
  slug: z.string().regex(/^[a-z0-9-]+$/, "slug 只能含 [a-z0-9-]"),
  title: z.string().min(1),
  description: z.string().min(1),
  icon: z.string().min(1),
  source: z.string().regex(/^github\.com\/[^/]+\/[^/]+$/, "source 必须形如 github.com/owner/repo"),
  branch: z.string().default("main"),
});

const ConfigSchema = z.object({
  site: z.object({
    name: z.string().min(1),
    description: z.string(),
    url: z.string().url(),
  }),
  repos: z.array(RepoSchema).min(1, "至少需要 1 个 repo"),
}).refine(
  (c) => new Set(c.repos.map(r => r.slug)).size === c.repos.length,
  { message: "repos[].slug 必须唯一" }
);

export async function loadConfig(path = "papyrus.config.json"): Promise<PapyrusConfig> {
  const raw = await readFile(path, "utf-8");
  const parsed = JSON.parse(raw);
  return ConfigSchema.parse(parsed) as PapyrusConfig;
}

export function parseSource(source: string): { owner: string; repo: string } {
  const m = source.match(/^github\.com\/([^/]+)\/([^/]+)$/);
  if (!m) throw new Error(`无效 source: ${source}`);
  return { owner: m[1], repo: m[2] };
}
```

- [ ] **Step 3: 写示例 `papyrus.config.json`**

```json
{
  "site": {
    "name": "Papyrus",
    "description": "我把笔记都搬到这里了",
    "url": "https://blog.example.com"
  },
  "repos": [
    {
      "slug": "tech-notes",
      "title": "技术笔记",
      "description": "前端、后端、基础设施的实战记录",
      "icon": "📘",
      "source": "github.com/withastro/docs",
      "branch": "main"
    }
  ]
}
```

> 注：示例先用 `withastro/docs` 这个真实公开 repo 当数据源，便于本地开发联调。用户使用时替换成自己的 repo。

- [ ] **Step 4: Commit**

```bash
git add src/lib/types.ts src/lib/config.ts papyrus.config.json
git commit -m "feat(config): 定义全局类型 + Zod 配置 schema + 示例 config"
```

---

## Task 5: tree.ts —— 扁平路径 → 嵌套树（TDD）

**Files:**
- Create: `vitest.config.ts`
- Create: `src/lib/tree.ts`
- Create: `tests/unit/tree.test.ts`

- [ ] **Step 1: 写 `vitest.config.ts`**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    include: ["tests/unit/**/*.test.ts"],
  },
  resolve: {
    alias: { "~": new URL("./src", import.meta.url).pathname },
  },
});
```

- [ ] **Step 2: 写失败的测试 `tests/unit/tree.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { buildTree } from "~/lib/tree";
import type { Article } from "~/lib/types";

const mk = (path: string[]): Article => ({
  repo: "x", path, title: path[path.length - 1], date: "2026-01-01", rawMarkdown: "",
});

describe("buildTree", () => {
  it("空数组返回空树", () => {
    expect(buildTree([])).toEqual([]);
  });

  it("单个根文件", () => {
    const t = buildTree([mk(["readme"])]);
    expect(t).toHaveLength(1);
    expect(t[0]).toMatchObject({ name: "readme", type: "file", path: ["readme"] });
  });

  it("二级嵌套", () => {
    const t = buildTree([mk(["frontend", "react"]), mk(["frontend", "vue"])]);
    expect(t).toHaveLength(1);
    expect(t[0]).toMatchObject({ name: "frontend", type: "dir", path: ["frontend"] });
    expect(t[0].children).toHaveLength(2);
    expect(t[0].children![0].name).toBe("react");
  });

  it("目录与文件混合，目录排前面", () => {
    const t = buildTree([
      mk(["readme"]),
      mk(["frontend", "react"]),
      mk(["backend", "db"]),
    ]);
    expect(t.map(n => n.name)).toEqual(["backend", "frontend", "readme"]);
    expect(t.map(n => n.type)).toEqual(["dir", "dir", "file"]);
  });

  it("同级文件按名字排序", () => {
    const t = buildTree([mk(["c"]), mk(["a"]), mk(["b"])]);
    expect(t.map(n => n.name)).toEqual(["a", "b", "c"]);
  });

  it("深度嵌套 3 层", () => {
    const t = buildTree([mk(["a", "b", "c"])]);
    expect(t[0].children![0].children![0]).toMatchObject({ name: "c", type: "file" });
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm test tests/unit/tree.test.ts`
Expected: 6 failed —— "Cannot find module ~/lib/tree" 或类似。

- [ ] **Step 4: 写 `src/lib/tree.ts`**

```ts
import type { Article, TreeNode } from "./types.js";

export function buildTree(articles: Article[]): TreeNode[] {
  const root: TreeNode[] = [];

  for (const article of articles) {
    let cursor = root;
    const segs = article.path;
    for (let i = 0; i < segs.length; i++) {
      const isLeaf = i === segs.length - 1;
      const name = segs[i];
      const fullPath = segs.slice(0, i + 1);
      let node = cursor.find(n => n.name === name && (isLeaf ? n.type === "file" : n.type === "dir"));
      if (!node) {
        node = isLeaf
          ? { name, type: "file", path: fullPath, article }
          : { name, type: "dir", path: fullPath, children: [] };
        cursor.push(node);
      }
      if (!isLeaf) cursor = node.children!;
    }
  }

  return sortTree(root);
}

function sortTree(nodes: TreeNode[]): TreeNode[] {
  nodes.sort((a, b) => {
    if (a.type !== b.type) return a.type === "dir" ? -1 : 1;
    return a.name.localeCompare(b.name);
  });
  for (const n of nodes) if (n.children) sortTree(n.children);
  return nodes;
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test tests/unit/tree.test.ts`
Expected: 6 passed。

- [ ] **Step 6: Commit**

```bash
git add vitest.config.ts src/lib/tree.ts tests/unit/tree.test.ts
git commit -m "feat(lib): 实现扁平路径→嵌套树并配套单测"
```

---

## Task 6: github.ts —— GitHub API 封装（TDD with MSW）

**Files:**
- Create: `tests/fixtures/repo-tree.json`
- Create: `tests/unit/github.test.ts`
- Create: `src/lib/github.ts`

- [ ] **Step 1: 写 fixture `tests/fixtures/repo-tree.json`**

```json
{
  "tree": [
    { "path": "README.md", "type": "blob", "sha": "abc1" },
    { "path": "docs", "type": "tree", "sha": "abc2" },
    { "path": "docs/intro.md", "type": "blob", "sha": "abc3" },
    { "path": "docs/setup.md", "type": "blob", "sha": "abc4" },
    { "path": "docs/img.png", "type": "blob", "sha": "abc5" }
  ]
}
```

- [ ] **Step 2: 写失败的测试 `tests/unit/github.test.ts`**

```ts
import { beforeAll, afterEach, afterAll, describe, it, expect } from "vitest";
import { setupServer } from "msw/node";
import { http, HttpResponse } from "msw";
import { readFile } from "node:fs/promises";
import { listMarkdownFiles, fetchFileContent, fetchLastCommitDate } from "~/lib/github";

const fixtureTree = JSON.parse(
  await readFile(new URL("../fixtures/repo-tree.json", import.meta.url), "utf-8")
);

const server = setupServer(
  http.get("https://api.github.com/repos/me/x/git/trees/main", () =>
    HttpResponse.json(fixtureTree)
  ),
  http.get("https://api.github.com/repos/me/x/contents/docs%2Fintro.md", () =>
    HttpResponse.json({ content: Buffer.from("# Hello\n\nbody").toString("base64"), encoding: "base64" })
  ),
  http.get("https://api.github.com/repos/me/x/commits", ({ request }) => {
    const url = new URL(request.url);
    if (url.searchParams.get("path") === "docs/intro.md") {
      return HttpResponse.json([{ commit: { committer: { date: "2026-05-21T10:00:00Z" } } }]);
    }
    return HttpResponse.json([]);
  }),
  http.get("https://api.github.com/repos/me/missing/git/trees/main", () =>
    HttpResponse.json({ message: "Not Found" }, { status: 404 })
  ),
);

beforeAll(() => server.listen({ onUnhandledRequest: "error" }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

describe("listMarkdownFiles", () => {
  it("递归拉取，仅返回 .md 文件路径", async () => {
    const files = await listMarkdownFiles({ owner: "me", repo: "x", branch: "main" });
    expect(files.sort()).toEqual(["README.md", "docs/intro.md", "docs/setup.md"]);
  });

  it("repo 不存在抛错", async () => {
    await expect(
      listMarkdownFiles({ owner: "me", repo: "missing", branch: "main" })
    ).rejects.toThrow(/Not Found|404/);
  });
});

describe("fetchFileContent", () => {
  it("解码 base64 内容", async () => {
    const text = await fetchFileContent({ owner: "me", repo: "x", path: "docs/intro.md" });
    expect(text).toBe("# Hello\n\nbody");
  });
});

describe("fetchLastCommitDate", () => {
  it("返回 ISO8601 日期", async () => {
    const d = await fetchLastCommitDate({ owner: "me", repo: "x", path: "docs/intro.md" });
    expect(d).toBe("2026-05-21T10:00:00Z");
  });

  it("无 commit 时回退到 epoch", async () => {
    const d = await fetchLastCommitDate({ owner: "me", repo: "x", path: "docs/setup.md" });
    expect(d).toBe("1970-01-01T00:00:00Z");
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm test tests/unit/github.test.ts`
Expected: 全部 fail，提示模块或导出不存在。

- [ ] **Step 4: 写 `src/lib/github.ts`**

```ts
import { Octokit } from "@octokit/rest";

const token = process.env.GITHUB_TOKEN;
export const octokit = new Octokit({ auth: token || undefined });

interface RepoRef { owner: string; repo: string; branch: string; }
interface FileRef { owner: string; repo: string; path: string; }

export async function listMarkdownFiles(ref: RepoRef): Promise<string[]> {
  const res = await octokit.git.getTree({
    owner: ref.owner,
    repo: ref.repo,
    tree_sha: ref.branch,
    recursive: "1",
  });
  return (res.data.tree ?? [])
    .filter(n => n.type === "blob" && typeof n.path === "string" && n.path.endsWith(".md"))
    .map(n => n.path!);
}

export async function fetchFileContent(ref: FileRef): Promise<string> {
  const res = await octokit.repos.getContent({
    owner: ref.owner, repo: ref.repo, path: ref.path,
  });
  const data = res.data as { content?: string; encoding?: string };
  if (!data.content) throw new Error(`No content: ${ref.path}`);
  return Buffer.from(data.content, (data.encoding ?? "base64") as BufferEncoding).toString("utf-8");
}

export async function fetchLastCommitDate(ref: FileRef): Promise<string> {
  const res = await octokit.repos.listCommits({
    owner: ref.owner, repo: ref.repo, path: ref.path, per_page: 1,
  });
  const c = res.data[0];
  return c?.commit?.committer?.date ?? "1970-01-01T00:00:00Z";
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm test tests/unit/github.test.ts`
Expected: 5 passed。

- [ ] **Step 6: Commit**

```bash
git add tests/fixtures/repo-tree.json tests/unit/github.test.ts src/lib/github.ts
git commit -m "feat(lib): GitHub API 封装（list/content/commit）+ MSW 单测"
```

---

## Task 7: content-loader —— 配置 × github → 归一化文章

**Files:**
- Create: `src/lib/content-loader.ts`
- Create: `tests/unit/content-loader.test.ts`

- [ ] **Step 1: 写测试 `tests/unit/content-loader.test.ts`**

```ts
import { describe, it, expect, vi } from "vitest";
import { loadAllArticles } from "~/lib/content-loader";

vi.mock("~/lib/github", () => ({
  listMarkdownFiles: vi.fn(async () => ["README.md", "frontend/react.md"]),
  fetchFileContent: vi.fn(async ({ path }) => `# Title of ${path}\n\nbody`),
  fetchLastCommitDate: vi.fn(async () => "2026-05-21T10:00:00Z"),
}));

const cfg = {
  site: { name: "T", description: "d", url: "https://t" },
  repos: [{
    slug: "tn", title: "TN", description: "d", icon: "📘",
    source: "github.com/me/tn", branch: "main",
  }],
};

describe("loadAllArticles", () => {
  it("把每个 repo 的 .md 转成 Article 列表", async () => {
    const arts = await loadAllArticles(cfg);
    expect(arts).toHaveLength(2);
    expect(arts[0]).toMatchObject({
      repo: "tn",
      path: ["README"],
      title: "README",
      date: "2026-05-21T10:00:00Z",
    });
    expect(arts[1].path).toEqual(["frontend", "react"]);
    expect(arts[1].rawMarkdown).toContain("Title");
  });

  it("frontmatter 覆盖标题与日期", async () => {
    const { fetchFileContent } = await import("~/lib/github");
    (fetchFileContent as any).mockImplementationOnce(async () =>
      "---\ntitle: 自定义标题\ndate: 2020-01-01\n---\n\n正文"
    );
    const arts = await loadAllArticles(cfg);
    expect(arts[0].title).toBe("自定义标题");
    expect(arts[0].date).toBe("2020-01-01T00:00:00.000Z");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm test tests/unit/content-loader.test.ts`
Expected: fail (模块未找到)。

- [ ] **Step 3: 写 `src/lib/content-loader.ts`**

```ts
import type { PapyrusConfig, Article } from "./types.js";
import { parseSource } from "./config.js";
import { listMarkdownFiles, fetchFileContent, fetchLastCommitDate } from "./github.js";

const FRONTMATTER = /^---\r?\n([\s\S]*?)\r?\n---\r?\n([\s\S]*)$/;

function parseFrontmatter(md: string): { meta: Record<string, string>; body: string } {
  const m = md.match(FRONTMATTER);
  if (!m) return { meta: {}, body: md };
  const meta: Record<string, string> = {};
  for (const line of m[1].split(/\r?\n/)) {
    const kv = line.match(/^(\w+):\s*(.+)$/);
    if (kv) meta[kv[1]] = kv[2].trim().replace(/^["']|["']$/g, "");
  }
  return { meta, body: m[2] };
}

function pathToSegments(filePath: string): string[] {
  return filePath.replace(/\.md$/i, "").split("/");
}

export async function loadAllArticles(config: PapyrusConfig): Promise<Article[]> {
  const all: Article[] = [];
  for (const repo of config.repos) {
    const { owner, repo: name } = parseSource(repo.source);
    const files = await listMarkdownFiles({ owner, repo: name, branch: repo.branch });
    for (const file of files) {
      const [raw, commitDate] = await Promise.all([
        fetchFileContent({ owner, repo: name, path: file }),
        fetchLastCommitDate({ owner, repo: name, path: file }),
      ]);
      const { meta, body } = parseFrontmatter(raw);
      const segs = pathToSegments(file);
      const title = meta.title ?? segs[segs.length - 1];
      const date = meta.date ? new Date(meta.date).toISOString() : commitDate;
      all.push({
        repo: repo.slug,
        path: segs,
        title,
        date,
        rawMarkdown: body,
        icon: meta.icon,
      });
    }
  }
  return all;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm test tests/unit/content-loader.test.ts`
Expected: 2 passed。

- [ ] **Step 5: Commit**

```bash
git add src/lib/content-loader.ts tests/unit/content-loader.test.ts
git commit -m "feat(lib): content-loader 组装 config 与 github API"
```

---

## Task 8: prebuild 缓存脚本

**Files:**
- Create: `scripts/prebuild.ts`

- [ ] **Step 1: 写 `scripts/prebuild.ts`**

```ts
#!/usr/bin/env tsx
import { mkdir, writeFile } from "node:fs/promises";
import { loadConfig } from "../src/lib/config.js";
import { loadAllArticles } from "../src/lib/content-loader.js";

async function main() {
  console.log("[prebuild] 读取 papyrus.config.json...");
  const config = await loadConfig();
  console.log(`[prebuild] 配置了 ${config.repos.length} 个 repo`);

  console.log("[prebuild] 拉取所有 repo 的 markdown...");
  const articles = await loadAllArticles(config);
  console.log(`[prebuild] 共 ${articles.length} 篇文章`);

  // 按 repo 校验：每个 repo 至少 1 篇
  for (const repo of config.repos) {
    const n = articles.filter(a => a.repo === repo.slug).length;
    if (n === 0) {
      console.warn(`[prebuild] ⚠️  repo ${repo.slug} 没拉到任何 .md（可能配错 source/branch）`);
    } else {
      console.log(`[prebuild]   - ${repo.slug}: ${n} 篇`);
    }
  }

  await mkdir(".cache", { recursive: true });
  await writeFile(
    ".cache/articles.json",
    JSON.stringify({ config, articles }, null, 2),
    "utf-8"
  );
  console.log("[prebuild] 缓存写入 .cache/articles.json");
}

main().catch((e) => {
  console.error("[prebuild] 失败:", e);
  process.exit(1);
});
```

- [ ] **Step 2: 验证脚本能跑（用示例 config 真实联调 GitHub）**

Run: `pnpm tsx scripts/prebuild.ts`
Expected:
- 看到 `[prebuild] 读取 papyrus.config.json...`
- 看到 `[prebuild] 配置了 1 个 repo`
- 看到拉到的 .md 数量（withastro/docs 有数百篇 .md）
- 生成 `.cache/articles.json`

> 如果遇到 rate limit 错误：在 `.env` 里填入 `GITHUB_TOKEN`，再跑 `set -a; source .env; set +a; pnpm tsx scripts/prebuild.ts`。

- [ ] **Step 3: Commit**

```bash
git add scripts/prebuild.ts
git commit -m "feat(build): prebuild 脚本将 repo 内容缓存至 .cache/articles.json"
```

---

## Task 9: markdown.ts —— remark/rehype 渲染管道（snapshot 测试）

**Files:**
- Create: `tests/fixtures/sample.md`
- Create: `tests/unit/markdown.test.ts`
- Create: `src/lib/markdown.ts`

> Astro 自带 markdown 编译能力，但我们需要在 `prebuild` 之外的脚本调用上下文中渲染（content-loader 拿到字符串后渲染），所以独立用 unified/remark/rehype 管道。

- [ ] **Step 1: 写 fixture `tests/fixtures/sample.md`**

```markdown
# Sample Title

正文段落。

## 二级标题

行内 `code` 与代码块：

\`\`\`ts
const x: number = 1;
\`\`\`

数学：$E = mc^2$

$$
\\sum_{i=1}^{n} i
$$

- 列表项 1
- 列表项 2
```

- [ ] **Step 2: 写测试 `tests/unit/markdown.test.ts`**

```ts
import { describe, it, expect } from "vitest";
import { readFile } from "node:fs/promises";
import { renderMarkdown } from "~/lib/markdown";

const fixture = await readFile(new URL("../fixtures/sample.md", import.meta.url), "utf-8");

describe("renderMarkdown", () => {
  it("产出 html 与 toc", async () => {
    const { html, toc } = await renderMarkdown(fixture);
    expect(typeof html).toBe("string");
    expect(toc.length).toBeGreaterThan(0);
  });

  it("代码块经过 shiki 高亮（含 token span）", async () => {
    const { html } = await renderMarkdown(fixture);
    expect(html).toContain('class="shiki');
    expect(html).toMatch(/<span\s+style="[^"]*color/);
  });

  it("标题带有 anchor id 和自链接", async () => {
    const { html } = await renderMarkdown(fixture);
    expect(html).toMatch(/<h2[^>]*id="二级标题"/);
    expect(html).toMatch(/<a[^>]+href="#二级标题"/);
  });

  it("数学公式被 KaTeX 渲染", async () => {
    const { html } = await renderMarkdown(fixture);
    expect(html).toContain('class="katex');
  });

  it("toc 包含 h2", async () => {
    const { toc } = await renderMarkdown(fixture);
    const h2 = toc.find(t => t.depth === 2);
    expect(h2?.text).toBe("二级标题");
    expect(h2?.id).toBe("二级标题");
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm test tests/unit/markdown.test.ts`
Expected: 模块未找到。

- [ ] **Step 4: 安装额外依赖**

Run: `pnpm add unified remark-parse remark-gfm remark-rehype rehype-stringify @shikijs/rehype mdast-util-to-string unist-util-visit`
Expected: 安装成功无 ERR。

- [ ] **Step 5: 写 `src/lib/markdown.ts`**

```ts
import { unified } from "unified";
import remarkParse from "remark-parse";
import remarkGfm from "remark-gfm";
import remarkMath from "remark-math";
import remarkRehype from "remark-rehype";
import rehypeKatex from "rehype-katex";
import rehypeSlug from "rehype-slug";
import rehypeAutolinkHeadings from "rehype-autolink-headings";
import rehypeShiki from "@shikijs/rehype";
import rehypeStringify from "rehype-stringify";
import { visit } from "unist-util-visit";
import { toString } from "mdast-util-to-string";
import type { TocItem } from "./types.js";

function extractToc() {
  return (tree: any, file: any) => {
    const toc: TocItem[] = [];
    visit(tree, "heading", (node: any) => {
      if (node.depth === 2 || node.depth === 3) {
        const text = toString(node);
        toc.push({ id: text, text, depth: node.depth });
      }
    });
    file.data.toc = toc;
  };
}

export async function renderMarkdown(md: string): Promise<{ html: string; toc: TocItem[] }> {
  const file = await unified()
    .use(remarkParse)
    .use(remarkGfm)
    .use(remarkMath)
    .use(extractToc)
    .use(remarkRehype, { allowDangerousHtml: true })
    .use(rehypeSlug)
    .use(rehypeAutolinkHeadings, { behavior: "wrap" })
    .use(rehypeKatex)
    .use(rehypeShiki, {
      themes: { light: "github-light", dark: "github-dark" },
      defaultColor: false,
    })
    .use(rehypeStringify, { allowDangerousHtml: true })
    .process(md);

  return {
    html: String(file),
    toc: (file.data.toc as TocItem[]) ?? [],
  };
}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `pnpm test tests/unit/markdown.test.ts`
Expected: 5 passed。如果 anchor id 因为 GitHub 风格 slug 不同而不匹配中文，把测试中的 `id="二级标题"` 改成实际产出的（rehype-slug 对中文一般保留原样，但请按真实输出修正）。

- [ ] **Step 7: 把 Shiki 双主题 CSS 写入 global.css 末尾**

Edit `src/styles/global.css`，在末尾追加：

```css
/* Shiki 双主题切换 */
html:not(.dark) .shiki span { color: var(--shiki-light) !important; }
html.dark .shiki span { color: var(--shiki-dark) !important; }
html:not(.dark) .shiki { background-color: var(--shiki-light-bg, var(--c-code-bg)) !important; }
html.dark .shiki { background-color: var(--shiki-dark-bg, var(--c-code-bg)) !important; }
```

- [ ] **Step 8: 引入 KaTeX 样式（写入 BaseLayout）**

Modify `src/layouts/BaseLayout.astro`，在 `<head>` 中加：

```astro
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css" />
```

- [ ] **Step 9: Commit**

```bash
git add tests/fixtures/sample.md tests/unit/markdown.test.ts src/lib/markdown.ts src/styles/global.css src/layouts/BaseLayout.astro package.json pnpm-lock.yaml
git commit -m "feat(lib): markdown 渲染管道（Shiki + KaTeX + autolink + TOC）"
```

---

## Task 10: 图片处理（下载相对路径图片）

**Files:**
- Create: `src/lib/images.ts`
- Modify: `src/lib/content-loader.ts`

- [ ] **Step 1: 写 `src/lib/images.ts`**

```ts
import { createHash } from "node:crypto";
import { mkdir, writeFile, access } from "node:fs/promises";
import { join, extname } from "node:path";

const PUBLIC_DIR = "public/images";

/**
 * 把 markdown 中所有相对路径图片下载到 public/images/{repo}/{hash}.{ext}
 * 并把 markdown 里的 src 改写为 /images/{repo}/{hash}.{ext}
 * 绝对 URL (http/https) 跳过
 */
export async function processImages(
  md: string,
  repoSlug: string,
  rawBaseUrl: string  // 例: https://raw.githubusercontent.com/me/x/main/docs/
): Promise<string> {
  const regex = /!\[([^\]]*)\]\(([^)]+)\)/g;
  const replacements: Array<{ from: string; to: string }> = [];

  for (const match of md.matchAll(regex)) {
    const [whole, alt, src] = match;
    if (/^https?:\/\//.test(src)) continue;

    const fullUrl = new URL(src, rawBaseUrl).toString();
    const localPath = await downloadAndCache(fullUrl, repoSlug);
    replacements.push({ from: whole, to: `![${alt}](${localPath})` });
  }

  let out = md;
  for (const r of replacements) out = out.replace(r.from, r.to);
  return out;
}

async function downloadAndCache(url: string, repoSlug: string): Promise<string> {
  const hash = createHash("sha1").update(url).digest("hex").slice(0, 12);
  const ext = extname(new URL(url).pathname).toLowerCase() || ".png";
  const dir = join(PUBLIC_DIR, repoSlug);
  const fileName = `${hash}${ext}`;
  const filePath = join(dir, fileName);
  const publicUrl = `/images/${repoSlug}/${fileName}`;

  try {
    await access(filePath);
    return publicUrl;
  } catch { /* not cached, download */ }

  const res = await fetch(url);
  if (!res.ok) {
    console.warn(`[images] 下载失败 ${url}: ${res.status}`);
    return url; // fallback
  }
  const buf = Buffer.from(await res.arrayBuffer());
  await mkdir(dir, { recursive: true });
  await writeFile(filePath, buf);
  return publicUrl;
}
```

- [ ] **Step 2: 在 content-loader 中接入图片处理**

Edit `src/lib/content-loader.ts`，在循环内调用 `processImages` 改写 `body`，并在文件头导入：

```ts
import { processImages } from "./images.js";
```

然后修改循环体（替换原 `all.push(...)` 之前的 body 处理）：

```ts
const rawBaseUrl = `https://raw.githubusercontent.com/${owner}/${name}/${repo.branch}/${file.replace(/[^/]+$/, "")}`;
const bodyWithImages = await processImages(body, repo.slug, rawBaseUrl);
// ...
all.push({
  repo: repo.slug,
  path: segs,
  title,
  date,
  rawMarkdown: bodyWithImages,
  icon: meta.icon,
});
```

- [ ] **Step 3: 重新跑 prebuild 验证（withastro/docs 有图）**

Run: `pnpm tsx scripts/prebuild.ts`
Expected: 部分文章会触发图片下载，`public/images/tech-notes/` 下能看到若干图片文件。

- [ ] **Step 4: Commit**

```bash
git add src/lib/images.ts src/lib/content-loader.ts
git commit -m "feat(lib): 构建期下载并改写 markdown 中的相对路径图片"
```

---

## Task 11: 首页（Repo Grid）

**Files:**
- Create: `src/layouts/HomeLayout.astro`
- Create: `src/components/RepoCard.astro`
- Modify: `src/pages/index.astro`

- [ ] **Step 1: 写 `src/layouts/HomeLayout.astro`**

```astro
---
import BaseLayout from "./BaseLayout.astro";
interface Props { title: string; description?: string; }
const { title, description } = Astro.props;
---
<BaseLayout title={title} description={description}>
  <main class="max-w-page mx-auto px-14 py-12">
    <slot />
  </main>
</BaseLayout>
```

- [ ] **Step 2: 写 `src/components/RepoCard.astro`**

```astro
---
interface Props {
  slug: string;
  title: string;
  description: string;
  icon: string;
  articleCount: number;
  latestDate: string;
}
const { slug, title, description, icon, articleCount, latestDate } = Astro.props;
const palettes = ["bg-blue-100 text-blue-600", "bg-green-100 text-green-600",
                  "bg-amber-100 text-amber-600", "bg-pink-100 text-pink-600",
                  "bg-violet-100 text-violet-600", "bg-cyan-100 text-cyan-600"];
const colorIdx = slug.split("").reduce((a, c) => a + c.charCodeAt(0), 0) % palettes.length;
const ago = (() => {
  const days = Math.floor((Date.now() - new Date(latestDate).getTime()) / 86400000);
  if (days === 0) return "今天更新";
  if (days < 7) return `${days} 天前更新`;
  if (days < 30) return `${Math.floor(days / 7)} 周前更新`;
  return `${Math.floor(days / 30)} 个月前更新`;
})();
---
<a href={`/${slug}`} class="block p-6 border border-border rounded-xl bg-bg hover:-translate-y-0.5 hover:shadow-lg transition">
  <div class={`w-9 h-9 rounded-lg flex items-center justify-center text-lg mb-3 ${palettes[colorIdx]}`}>
    {icon}
  </div>
  <h3 class="font-semibold mb-1">{title}</h3>
  <p class="text-sm text-text-mute leading-relaxed mb-3">{description}</p>
  <div class="text-xs text-text-faint flex gap-3">
    <span>{articleCount} 篇</span>
    <span>·</span>
    <span>{ago}</span>
  </div>
</a>
```

- [ ] **Step 3: 改写 `src/pages/index.astro`**

```astro
---
import HomeLayout from "~/layouts/HomeLayout.astro";
import RepoCard from "~/components/RepoCard.astro";
import { readFile } from "node:fs/promises";
import type { PapyrusConfig, Article } from "~/lib/types";

const cache = JSON.parse(await readFile(".cache/articles.json", "utf-8")) as {
  config: PapyrusConfig; articles: Article[];
};
const { config, articles } = cache;

const repoStats = config.repos.map(r => {
  const list = articles.filter(a => a.repo === r.slug);
  const latest = list.reduce((acc, a) => a.date > acc ? a.date : acc, "1970-01-01T00:00:00Z");
  return { ...r, articleCount: list.length, latestDate: latest };
});
---
<HomeLayout title={config.site.name} description={config.site.description}>
  <section class="text-center mb-10">
    <h1 class="text-4xl font-extrabold tracking-tight mb-2">欢迎来到 {config.site.name}</h1>
    <p class="text-text-mute">{config.site.description}</p>
  </section>
  <div class="grid grid-cols-3 gap-5">
    {repoStats.map(r => <RepoCard {...r} />)}
  </div>
</HomeLayout>
```

- [ ] **Step 4: 启动 dev server 肉眼验证**

Run: `pnpm dev`
Expected: 访问 `http://localhost:4321`，看到 1 个 `RepoCard`（示例 config 里只有 1 个 repo），点击进入 `/tech-notes` 会 404（下一任务实现）。

- [ ] **Step 5: Commit**

```bash
git add src/layouts/HomeLayout.astro src/components/RepoCard.astro src/pages/index.astro
git commit -m "feat(page): 首页 RepoCard Grid 渲染"
```

---

## Task 12: Repo 入口页（侧栏树 + 文章列表）

**Files:**
- Create: `src/layouts/RepoLayout.astro`
- Create: `src/components/RepoTree.astro`
- Create: `src/components/ArticleList.astro`
- Create: `src/pages/[repo]/index.astro`

- [ ] **Step 1: 写 `src/components/RepoTree.astro`**

```astro
---
import type { TreeNode } from "~/lib/types";
interface Props { nodes: TreeNode[]; repoSlug: string; currentPath?: string[]; }
const { nodes, repoSlug, currentPath = [] } = Astro.props;
const currentJoined = currentPath.join("/");
---
<ul class="text-sm">
  {nodes.map(node => {
    const href = `/${repoSlug}/${node.path.join("/")}`;
    const isActive = node.type === "file" && node.path.join("/") === currentJoined;
    if (node.type === "dir") {
      return (
        <li class="my-1">
          <div class="text-text-faint text-[11px] uppercase tracking-wider px-2 py-1">{node.name}</div>
          <div class="pl-3">
            <Astro.self nodes={node.children!} repoSlug={repoSlug} currentPath={currentPath} />
          </div>
        </li>
      );
    }
    return (
      <li>
        <a href={href} class={`block py-1 px-2 rounded ${isActive ? 'bg-accent/10 text-accent font-medium' : 'text-text-mute hover:bg-bg-elev'}`}>
          {node.name}
        </a>
      </li>
    );
  })}
</ul>
```

- [ ] **Step 2: 写 `src/components/ArticleList.astro`**

```astro
---
import type { Article } from "~/lib/types";
interface Props { articles: Article[]; }
const { articles } = Astro.props;
---
<ul>
  {articles.map(a => (
    <li class="py-4 border-b border-border last:border-0">
      <div class="text-xs text-text-faint mb-1">
        {a.path.slice(0, -1).join(" / ")} · {a.date.slice(0, 10)}
      </div>
      <a href={`/${a.repo}/${a.path.join("/")}`} class="text-base font-semibold hover:text-accent">
        {a.title}
      </a>
    </li>
  ))}
</ul>
```

- [ ] **Step 3: 写 `src/layouts/RepoLayout.astro`**

```astro
---
import BaseLayout from "./BaseLayout.astro";
import type { TreeNode } from "~/lib/types";
import RepoTree from "~/components/RepoTree.astro";
interface Props { title: string; tree: TreeNode[]; repoSlug: string; }
const { title, tree, repoSlug } = Astro.props;
---
<BaseLayout title={title}>
  <div class="max-w-page mx-auto grid grid-cols-[240px_1fr] gap-9 px-14 py-10">
    <aside class="border-r border-border pr-4">
      <RepoTree nodes={tree} repoSlug={repoSlug} />
    </aside>
    <main><slot /></main>
  </div>
</BaseLayout>
```

- [ ] **Step 4: 写 `src/pages/[repo]/index.astro`**

```astro
---
import RepoLayout from "~/layouts/RepoLayout.astro";
import ArticleList from "~/components/ArticleList.astro";
import { readFile } from "node:fs/promises";
import { buildTree } from "~/lib/tree";
import type { PapyrusConfig, Article } from "~/lib/types";

export async function getStaticPaths() {
  const cache = JSON.parse(await readFile(".cache/articles.json", "utf-8")) as {
    config: PapyrusConfig; articles: Article[];
  };
  return cache.config.repos.map(r => ({
    params: { repo: r.slug },
    props: {
      repo: r,
      articles: cache.articles.filter(a => a.repo === r.slug),
    },
  }));
}

const { repo, articles } = Astro.props;
const sorted = [...articles].sort((a, b) => b.date.localeCompare(a.date));
const tree = buildTree(articles);
---
<RepoLayout title={repo.title} tree={tree} repoSlug={repo.slug}>
  <h1 class="text-2xl font-bold mb-1">{repo.title}</h1>
  <p class="text-text-mute mb-6">{repo.description}</p>
  <div class="text-xs text-text-faint mb-2">{articles.length} 篇 · 按时间倒序</div>
  <ArticleList articles={sorted} />
</RepoLayout>
```

- [ ] **Step 5: 启动 dev 验证**

Run: `pnpm dev`
Expected: 首页点 RepoCard → 进入 `/tech-notes`，左侧出现树状导航，右侧出现文章列表。

- [ ] **Step 6: Commit**

```bash
git add src/components/RepoTree.astro src/components/ArticleList.astro src/layouts/RepoLayout.astro src/pages/[repo]/index.astro
git commit -m "feat(page): Repo 入口页（树状侧栏 + 文章列表）"
```

---

## Task 13: 文章页（三栏 + Prose + TOC + 图片放大）

**Files:**
- Create: `src/layouts/ArticleLayout.astro`
- Create: `src/components/ArticleTOC.astro`
- Create: `src/components/Prose.astro`
- Create: `src/pages/[repo]/[...path].astro`

- [ ] **Step 1: 写 `src/components/ArticleTOC.astro`**

```astro
---
import type { TocItem } from "~/lib/types";
interface Props { items: TocItem[]; }
const { items } = Astro.props;
---
<nav class="sticky top-6 text-xs">
  <div class="text-text-faint uppercase tracking-wider text-[11px] mb-2">On this page</div>
  <ul>
    {items.map(t => (
      <li class={`py-1 ${t.depth === 3 ? 'pl-4 text-[12px]' : ''} text-text-mute`}>
        <a href={`#${t.id}`} class="hover:text-accent">{t.text}</a>
      </li>
    ))}
  </ul>
</nav>
```

- [ ] **Step 2: 写 `src/components/Prose.astro`**

```astro
---
interface Props { html: string; }
const { html } = Astro.props;
---
<article class="prose" set:html={html} />
<script>
  import mediumZoom from "medium-zoom";
  mediumZoom("article.prose img", { background: "rgba(0,0,0,0.85)", margin: 24 });
</script>
```

- [ ] **Step 3: 写 `src/layouts/ArticleLayout.astro`**

```astro
---
import BaseLayout from "./BaseLayout.astro";
import RepoTree from "~/components/RepoTree.astro";
import ArticleTOC from "~/components/ArticleTOC.astro";
import type { TreeNode, TocItem } from "~/lib/types";

interface Props {
  title: string;
  tree: TreeNode[];
  repoSlug: string;
  currentPath: string[];
  toc: TocItem[];
}
const { title, tree, repoSlug, currentPath, toc } = Astro.props;
---
<BaseLayout title={title}>
  <div class="max-w-page mx-auto grid grid-cols-[240px_1fr_220px] gap-9 px-14 py-10">
    <aside class="border-r border-border pr-4">
      <RepoTree nodes={tree} repoSlug={repoSlug} currentPath={currentPath} />
    </aside>
    <main class="max-w-prose">
      <slot />
    </main>
    <aside class="border-l border-border pl-4">
      <ArticleTOC items={toc} />
    </aside>
  </div>
</BaseLayout>
```

- [ ] **Step 4: 写 `src/pages/[repo]/[...path].astro`**

```astro
---
import ArticleLayout from "~/layouts/ArticleLayout.astro";
import Prose from "~/components/Prose.astro";
import { readFile } from "node:fs/promises";
import { buildTree } from "~/lib/tree";
import { renderMarkdown } from "~/lib/markdown";
import type { PapyrusConfig, Article } from "~/lib/types";

export async function getStaticPaths() {
  const cache = JSON.parse(await readFile(".cache/articles.json", "utf-8")) as {
    config: PapyrusConfig; articles: Article[];
  };
  return cache.articles.map(a => ({
    params: { repo: a.repo, path: a.path.join("/") },
    props: {
      article: a,
      repoArticles: cache.articles.filter(x => x.repo === a.repo),
    },
  }));
}

const { article, repoArticles } = Astro.props;
const { html, toc } = await renderMarkdown(article.rawMarkdown);
const tree = buildTree(repoArticles);
---
<ArticleLayout
  title={article.title}
  tree={tree}
  repoSlug={article.repo}
  currentPath={article.path}
  toc={toc}
>
  <div class="text-xs text-text-faint mb-2">
    {article.repo} / {article.path.join("/")}.md
  </div>
  <h1 class="text-3xl font-bold mb-1">{article.title}</h1>
  <div class="text-sm text-text-mute mb-6 pb-4 border-b border-border">
    最后更新 {article.date.slice(0, 10)}
  </div>
  <Prose html={html} />
</ArticleLayout>
```

- [ ] **Step 5: 启动 dev 验证**

Run: `pnpm dev`
Expected: 从首页 → repo 页 → 点任一文章，看到三栏布局；正文有代码高亮 / 标题锚点 / 右侧 TOC 高亮当前位置；图片点击能放大；暗色模式下代码块和正文颜色都对。

- [ ] **Step 6: Commit**

```bash
git add src/components/ArticleTOC.astro src/components/Prose.astro src/layouts/ArticleLayout.astro src/pages/[repo]/[...path].astro
git commit -m "feat(page): 文章页（三栏 + Prose + TOC + 图片放大）"
```

---

## Task 14: 404 页

**Files:**
- Create: `src/pages/404.astro`

- [ ] **Step 1: 写 `src/pages/404.astro`**

```astro
---
import BaseLayout from "~/layouts/BaseLayout.astro";
---
<BaseLayout title="404 — Papyrus">
  <main class="max-w-page mx-auto px-14 py-24 text-center">
    <div class="text-6xl font-extrabold text-text-faint mb-4">404</div>
    <p class="text-text-mute mb-6">这一页找不到了 — 也许该文章已被改名或删除</p>
    <a href="/" class="text-accent hover:underline">← 回到首页</a>
  </main>
</BaseLayout>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/404.astro
git commit -m "feat(page): 404 页"
```

---

## Task 15: 搜索（Pagefind + ⌘K 弹窗）

**Files:**
- Create: `src/components/SearchModal.astro`
- Modify: `src/components/SiteHeader.astro`
- Modify: `package.json`（确认 build 脚本含 `pagefind --site dist`，Task 1 已配）

> Pagefind 在 `astro build` 之后扫描产物 HTML 自动建索引；前端通过 `window.pagefind` 调用。

- [ ] **Step 1: 写 `src/components/SearchModal.astro`**

```astro
---
---
<div id="search-modal" class="fixed inset-0 z-50 hidden">
  <div class="absolute inset-0 bg-black/60" id="search-backdrop"></div>
  <div class="relative max-w-xl mx-auto mt-24 bg-bg border border-border rounded-xl shadow-2xl overflow-hidden">
    <input
      id="search-input"
      type="text"
      placeholder="搜索文章..."
      class="w-full px-5 py-4 bg-transparent text-text outline-none border-b border-border"
      autocomplete="off"
    />
    <div id="search-results" class="max-h-[60vh] overflow-y-auto p-2"></div>
  </div>
</div>

<script>
  const modal = document.getElementById("search-modal")!;
  const input = document.getElementById("search-input") as HTMLInputElement;
  const results = document.getElementById("search-results")!;
  const backdrop = document.getElementById("search-backdrop")!;
  const openBtn = document.getElementById("open-search");

  let pagefind: any = null;
  async function ensurePagefind() {
    if (!pagefind) {
      // @ts-ignore
      pagefind = await import(/* @vite-ignore */ "/pagefind/pagefind.js");
      pagefind.options({ excerptLength: 30 });
      await pagefind.init();
    }
    return pagefind;
  }

  function open() {
    modal.classList.remove("hidden");
    input.value = "";
    results.innerHTML = "";
    setTimeout(() => input.focus(), 0);
  }
  function close() {
    modal.classList.add("hidden");
  }

  openBtn?.addEventListener("click", open);
  backdrop.addEventListener("click", close);
  window.addEventListener("keydown", (e) => {
    if ((e.metaKey || e.ctrlKey) && e.key === "k") { e.preventDefault(); open(); }
    if (e.key === "Escape" && !modal.classList.contains("hidden")) close();
  });

  let last = 0;
  input.addEventListener("input", async () => {
    const q = input.value.trim();
    const tick = ++last;
    if (!q) { results.innerHTML = ""; return; }
    const pf = await ensurePagefind();
    const search = await pf.search(q);
    if (tick !== last) return;
    const items = await Promise.all(search.results.slice(0, 8).map((r: any) => r.data()));
    results.innerHTML = items.map(d => `
      <a href="${d.url}" class="block px-3 py-2 rounded hover:bg-bg-elev">
        <div class="text-sm font-medium text-text">${d.meta?.title ?? d.url}</div>
        <div class="text-xs text-text-mute mt-1">${d.excerpt}</div>
      </a>
    `).join("");
  });
</script>
```

- [ ] **Step 2: 在 BaseLayout 中挂载 SearchModal**

Modify `src/layouts/BaseLayout.astro`，在 `<SiteHeader />` 后面加：

```astro
<SearchModal />
```

并在顶部 import 处添加：
```astro
import SearchModal from "~/components/SearchModal.astro";
```

- [ ] **Step 3: 给 Prose 容器加 `data-pagefind-body`，让 Pagefind 只索引文章正文**

Edit `src/components/Prose.astro` 把 `<article class="prose"` 改为：
```astro
<article class="prose" data-pagefind-body set:html={html} />
```

并在 `src/pages/[repo]/[...path].astro` 文章标题处加 `data-pagefind-meta="title"`：
```astro
<h1 class="text-3xl font-bold mb-1" data-pagefind-meta="title">{article.title}</h1>
```

- [ ] **Step 4: 构建并 preview 验证搜索**

Run: `pnpm build && pnpm preview`
Expected: 看到 `pagefind --site dist` 输出，生成 `dist/pagefind/` 目录；浏览器访问预览站，按 ⌘K 弹窗出现，输入关键字能返回结果，点击跳到对应文章。

- [ ] **Step 5: Commit**

```bash
git add src/components/SearchModal.astro src/layouts/BaseLayout.astro src/components/Prose.astro src/pages/[repo]/[...path].astro
git commit -m "feat(search): Pagefind 集成 + ⌘K 弹窗"
```

---

## Task 16: Playwright Smoke E2E

**Files:**
- Create: `playwright.config.ts`
- Create: `tests/e2e/smoke.spec.ts`

- [ ] **Step 1: 安装浏览器**

Run: `pnpm exec playwright install chromium`
Expected: 下载 Chromium。

- [ ] **Step 2: 写 `playwright.config.ts`**

```ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "tests/e2e",
  use: { baseURL: "http://localhost:4321" },
  webServer: {
    command: "pnpm build && pnpm preview --port 4321",
    url: "http://localhost:4321",
    timeout: 180_000,
    reuseExistingServer: !process.env.CI,
  },
  projects: [{ name: "chromium", use: { browserName: "chromium", viewport: { width: 1440, height: 900 } } }],
});
```

- [ ] **Step 3: 写 `tests/e2e/smoke.spec.ts`**

```ts
import { test, expect } from "@playwright/test";

test("smoke: 首页 → repo → 文章 → 搜索 → 暗色", async ({ page }) => {
  await page.goto("/");
  await expect(page.locator("h1")).toContainText("欢迎");

  // 进入第一个 repo
  const firstCard = page.locator("a[href^='/']").filter({ hasText: /篇/ }).first();
  await firstCard.click();
  await expect(page.locator("aside")).toBeVisible();

  // 进入第一篇文章
  const firstArticle = page.locator("aside ul a").first();
  await firstArticle.click();
  await expect(page.locator("article.prose")).toBeVisible();
  await expect(page.locator("nav").filter({ hasText: "On this page" })).toBeVisible();

  // ⌘K 搜索
  await page.keyboard.press("Meta+k");
  await expect(page.locator("#search-input")).toBeFocused();
  await page.locator("#search-input").fill("astro");
  await page.waitForTimeout(800);
  // 不强行断言有结果（取决于 fixture 数据），仅断言面板存在
  await expect(page.locator("#search-results")).toBeVisible();
  await page.keyboard.press("Escape");

  // 暗色模式
  await page.locator("#theme-toggle").click();
  await expect(page.locator("html")).toHaveClass(/dark/);
});
```

- [ ] **Step 4: 跑 e2e**

Run: `pnpm test:e2e`
Expected: 1 个测试 passed。如果搜索那步因索引为空失败，把"输入关键字"那行换成命中实际文章的词（如 README 内容里有的词）。

- [ ] **Step 5: Commit**

```bash
git add playwright.config.ts tests/e2e/smoke.spec.ts
git commit -m "test(e2e): Playwright smoke 覆盖首页→repo→文章→搜索→暗色"
```

---

## Task 17: 部署文档与 webhook 接入

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 重写 `README.md`**

```markdown
# Papyrus

把多个 GitHub repo 里的 markdown 笔记，零运维地聚合成一个静态博客。

## 本地开发

\`\`\`bash
pnpm install
cp .env.example .env  # 填入 GITHUB_TOKEN（避免 rate limit）
pnpm dev              # 注意：dev 不会自动跑 prebuild，先跑一次：
pnpm tsx scripts/prebuild.ts
pnpm dev
\`\`\`

## 构建

\`\`\`bash
pnpm build
pnpm preview   # 本地预览
\`\`\`

构建过程：prebuild (拉取 GitHub) → astro build → pagefind 建索引。

## 配置

编辑 `papyrus.config.json`：

\`\`\`json
{
  "site": { "name": "...", "description": "...", "url": "..." },
  "repos": [
    { "slug": "tech-notes", "title": "...", "description": "...",
      "icon": "📘", "source": "github.com/me/tech-notes", "branch": "main" }
  ]
}
\`\`\`

约束：`slug` 在站内唯一；`source` 必须公开 repo。

## 部署到 Vercel

1. Push 本仓库到 GitHub
2. 在 Vercel 新建项目，导入该 repo
3. 环境变量：`GITHUB_TOKEN`（值为一个 PAT，`public_repo` 权限）
4. Build Command 保持默认（`pnpm build`），Output Directory 设 `dist`
5. 在 Vercel 项目 → Settings → Git → Deploy Hooks 创建一个 hook，复制 URL

## 设置自动更新（每个内容 repo 一次性配置）

对每个 `papyrus.config.json` 中列出的 repo：

1. 打开该 repo → Settings → Webhooks → Add webhook
2. Payload URL：上一步 Vercel Deploy Hook URL
3. Content type：`application/json`
4. Events：仅勾选 `push`

效果：任何 push 到该 repo → 1–3 分钟内 Vercel 触发新构建 → 站点更新。

## 定时兜底

在 Vercel 项目里另设一个 daily cron deploy（Project → Settings → Cron Jobs），防止漏 webhook。

## 加入新 repo

编辑 `papyrus.config.json` 添加一项 → 给该 repo 配 webhook → push 任意文件触发首次构建。

## 验收清单

部署后人工跑一遍：

- [ ] 首页 N 个 repo 卡片显示正确
- [ ] 暗色模式切换无白闪
- [ ] 任意 repo 任意文章页能打开，代码高亮 / 数学 / 图片都对
- [ ] ⌘K 搜索能搜到内容
- [ ] 在某 repo push 新 .md → 1-3 分钟后站点可访问
\`\`\`
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: 部署、配置、webhook 设置文档"
```

---

## Self-Review

### Spec 覆盖检查

| Spec 章节 | 任务 |
|---|---|
| 1. 项目目标与定位 | 全局，README + 设计 |
| 2. 技术栈 | Task 1 |
| 3.1 整体数据流 | Task 6, 7, 8 |
| 3.2 配置文件 | Task 4 |
| 3.3 新增/改名文件同步逻辑 | Task 6, 7, 8 |
| 3.4 构建触发（webhook） | Task 17 |
| 3.5 API 速率限制（GITHUB_TOKEN） | Task 1, 17 |
| 4.1 URL 结构 | Task 11, 12, 13 |
| 4.2 4 种 layouts | Task 3, 11, 12, 13, 15 |
| 4.3 视口（≥1280px） | Task 3 (viewport meta) |
| 5.1 图片处理 | Task 10, 13 |
| 5.2 元数据来源 | Task 7 (frontmatter override) |
| 5.3 JS 预算 | Task 13, 15 (按需 import) |
| 6.1 颜色 tokens | Task 2 |
| 6.2 字体 | Task 2 |
| 6.3 字号节奏 | Task 2 (prose.css) |
| 6.4 暗色模式 | Task 3, 9 (Shiki) |
| 6.5 容器 / 节奏 | Task 2 (max-w), Task 11–13 |
| 7. 项目结构 | 全部任务 |
| 8.1 单元测试 | Task 5, 6, 7, 9 |
| 8.2 E2E smoke | Task 16 |
| 8.3 Fixtures | Task 6, 9 |
| 8.4 构建期校验 | Task 4, 8 |
| 8.5 验收清单 | Task 17 |

→ 所有 spec 章节都有对应任务覆盖。

### Placeholder Scan

✅ 无 TBD / TODO / "稍后实现" / "处理边界情况"等 placeholder。
✅ 每个 step 都有具体代码或具体命令。

### Type Consistency

- `Article` 类型在 Task 4 定义（`{ repo, path, title, date, rawMarkdown, icon? }`），Task 5/7/10/11/12/13 均一致使用。
- `TreeNode` 类型在 Task 4 定义，Task 5 实现 `buildTree`，Task 12/13 使用一致。
- `TocItem` 类型在 Task 4 定义，Task 9 实现 toc 提取，Task 13 使用一致。
- `loadConfig` Task 4 定义；`loadAllArticles` Task 7 定义；`renderMarkdown` Task 9 定义；`processImages` Task 10 定义；签名前后一致。
- 函数名 `listMarkdownFiles` / `fetchFileContent` / `fetchLastCommitDate` 在 Task 6 测试、实现，Task 7 调用，全部一致。

→ 无类型不一致问题。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-05-papyrus.md`. Two execution options:

**1. Subagent-Driven (recommended)** — 每个任务派发一个全新 subagent，任务之间评审，迭代快、上下文干净

**2. Inline Execution** — 在本会话内顺序执行任务，按 checkpoint 暂停评审

哪种？
